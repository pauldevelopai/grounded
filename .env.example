# Database
DATABASE_URL=postgresql://toolkitrag:changeme@db:5432/toolkitrag
POSTGRES_USER=toolkitrag
POSTGRES_PASSWORD=changeme
POSTGRES_DB=toolkitrag

# Auth
JWT_SECRET_KEY=generate-a-secure-random-key-here-use-openssl-rand-hex-32
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=7

# OpenAI
OPENAI_API_KEY=sk-your-api-key-here

# App
APP_ENV=development
APP_DEBUG=true
CORS_ORIGINS=["http://localhost:8000"]

# Admin (first user - change these!)
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=changeme123

# Toolkit document path (mounted in docker-compose)
TOOLKIT_DOCX_PATH=/mnt/data/DONE2.docx
